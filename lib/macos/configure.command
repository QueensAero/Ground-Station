#!/bin/bash
# Yes... this script has only one command.
# It is just for convenience for people who are not familiar with
# using a command line interface.
cd $(dirname "$0")
cp .classpath.example ../../.classpath
